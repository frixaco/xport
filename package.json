{
  "name": "xport",
  "version": "0.1.0",
  "private": true,
  "workspaces": [
    "web",
    "cli"
  ],
  "scripts": {
    "dev": "pnpm --filter web dev",
    "build": "pnpm --filter web build",
    "start": "pnpm --filter web start",
    "lint": "pnpm --filter web lint && pnpm --filter cli lint",
    "lint:fix": "pnpm --filter web lint:fix && pnpm --filter cli lint:fix",
    "format": "pnpm run format:root && pnpm --filter web format && pnpm --filter cli format",
    "format:check": "pnpm run format:root:check && pnpm --filter web format:check && pnpm --filter cli format:check",
    "format:root": "oxfmt --ignore-path .gitignore --write package.json pnpm-workspace.yaml .oxlintrc.json .oxfmtrc.json",
    "format:root:check": "oxfmt --ignore-path .gitignore --check package.json pnpm-workspace.yaml .oxlintrc.json .oxfmtrc.json",
    "check": "pnpm run format:check && pnpm run lint",
    "db:migrate": "pnpm --filter web db:migrate",
    "db:generate": "pnpm --filter web db:generate"
  },
  "devDependencies": {
    "@types/node": "^24",
    "oxfmt": "^0.55.0",
    "oxlint": "^1.70.0",
    "oxlint-tailwindcss": "^1.3.3",
    "typescript": "^6.0.3"
  },
  "engines": {
    "node": "24.x",
    "pnpm": ">=10"
  },
  "packageManager": "pnpm@10.33.0"
}
