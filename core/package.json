{
  "name": "@xport/core",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "types": "./src/index.ts",
  "exports": {
    ".": "./src/index.ts"
  },
  "scripts": {
    "test": "node --test test/account-replies.test.ts",
    "lint": "oxlint",
    "lint:fix": "oxlint --fix",
    "check": "pnpm run format:check && pnpm run lint",
    "format:check": "oxfmt --check .",
    "format": "oxfmt --write ."
  }
}
